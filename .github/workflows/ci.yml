name: CI

on:
  push:
    branches: [ main, dev ]
  workflow_dispatch:

jobs:
  ci:
    uses: bigbang1112/workflows/.github/workflows/ci.yml@main
    with:
      dotnet-version: 10.x.x
      workloads: wasm-tools